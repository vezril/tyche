{
  "name": "@ynab-clone/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Types shared across the API boundary (ADR-002), incl. the branded Milliunits money type (ADR-004).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
